# AGENTS.md — Quy tắc Dự án external-server-timer-v34

## Quy tắc Bắt buộc: Focus 100% vào Dự án Mới v34

1. **Thư mục làm việc chính:**
   * Mọi file mới, sửa đổi code, migrations, controllers, services, jobs và lệnh terminal cho phiên bản V34 phải được thực hiện trực tiếp tại:
     `D:\GiaVi\Viettel\2026\external-server-timer-v34`

2. **Quy tắc Tham chiếu Codebase Gốc:**
   * Thư mục dự án gốc (`d:\GiaVi\ServBay\www\external-server-timer`) CHỈ DÙNG ĐỂ ĐỌC VÀ THAM KHẢO logic nghiệp vụ.
   * Tuyệt đối KHÔNG sửa đổi, xóa hay tạo file code mới vào dự án gốc.

3. **Chấp thuận Kế hoạch trước khi Code:**
   * Trình bày kế hoạch thực thi từng hạng mục và chỉ tiến hành viết code khi người dùng đồng ý.

4. **Chặn Browser và Terminal Tự Chạy:**
   * Tuyệt đối KHÔNG tự chạy terminal command mà không có sự phê duyệt (Approve) từ người dùng.
   * Không tự ý dùng browser agent.

5. **Ngôn ngữ:**
   * Giao tiếp hoàn toàn bằng Tiếng Việt.
